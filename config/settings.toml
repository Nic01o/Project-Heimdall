# User-tunable overrides only — the diff from each instance's schema
# defaults (see get_settings_schema()). Written by the web UI through
# update_settings(); safe to hand-edit while the daemon is stopped.
